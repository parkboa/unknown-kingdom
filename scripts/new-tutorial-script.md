좌표 표기: `(x,y)` 0 기반. 왼쪽 위가 `(0,0)`, 오른쪽 아래가 `(8,8)`입니다.

1. <대화창>대국의 세계에 오신 것을 환영합니다! 대국 게임에 대해서 안내해 드리겠습니다.
2. <대화창>이 대국은 상대의 왕을 먼저 잡거나 영토를 많이 차지한 플레이어가 승리합니다.
3. <대화창>상대 왕을 잡으면 즉시 승리, 왕을 잡지 못한 채 승부가 끝나면 더 많은 영토를 가진 쪽이 승리합니다.
4. <대화창>흑돌이 선공, 백돌이 후공으로 대국을 시작합니다.
5. <설명창>시작하기를 누르세요.
6. <설명창>먼저 왕을 `(4,7)`에 배치하세요.
7. <설명창>왕이 배치되면 왕을 둘러싼 성역이 나타납니다. 왕과 주변 8칸이 성역입니다.
8. 다음 화살표를 누르면 <설명창>자신이 돌을 네 개 더 놓을 때까지 상대는 성역 안에 배치할 수 없습니다. 설명과 동시에 <스크립트 동작> 흑 왕 주변 `(4,0)`, `(3,1)`, `(5,1)`, `(4,2)`와 백 왕 주변 `(4,6)`, `(3,7)`, `(5,7)`, `(4,8)`에 병사를 번갈아 자동 배치합니다.
9. <설명창>돌의 상하좌우 중 열려 있는 칸은 활로라고 합니다. 활로가 모두 막히면 그 돌은 잡힙니다.
10. <스크립트 동작> 포획 대상 흑돌을 `(4,5)`에 먼저 놓고, 자동 백돌 `(3,5)`, 흑 진영 성벽의 흑돌 `(1,0)`, 자동 백돌 `(5,5)`, 백 진영 성벽의 흑돌 `(1,8)` 순서로 자동 배치합니다. 기존 백돌 `(4,6)`과 함께 포획 대상 흑돌의 활로 세 곳을 막습니다.
11. <설명창>돌은 상하좌우의 활로가 모두 막히면 포획됩니다. 표시된 칸에 백 병사를 놓아 흑 병사를 잡으세요.
12. 백돌을 놓을 `(4,4)`를 하이라이트합니다.

13. <설명창>흑 병사가 포획되었습니다.
14. 다음을 누르면 <스크립트 동작> 흑돌 `(5,4)`, 백돌 `(0,0)`, 흑돌 `(6,5)`, 백돌 `(1,1)`, 흑돌 `(5,6)` 순서로 자동 배치합니다.
15. <설명창>자기 성벽에 닿은 돌은 성벽쪽에 활로 하나를 얻습니다. 표시된 칸에 병사를 놓아보세요. 백 병사를 놓을 `(2,0)`을 하이라이트합니다.
16. 사용자가 `(2,0)`에 백 병사를 놓으면 <설명창>흑 병사는 자기 성벽에서 활로를 얻어 포획되지 않았습니다.
17. 다음을 누르면 <스크립트 동작> 흑돌 `(3,6)`, 백돌 `(0,8)`, 흑돌 `(5,8)`, 백돌 `(1,7)`, 흑돌 `(6,7)` 순서로 자동 배치합니다.
18. <설명창>상대 성벽에 닿은 돌은 어떻게 될까요? 표시된 칸에 병사를 놓아보세요. 백 병사를 놓을 `(2,8)`을 하이라이트합니다.
19. 사용자가 `(2,8)`에 백 병사를 놓으면 <설명창>흑 병사는 백의 성벽에 막혀 포획되었습니다.
20. 다음을 누르면 <설명창>특수 유닛을 배워 봅시다. 대국 시작 후 자신의 돌을 다섯 번 놓으면 특수 유닛을 사용할 수 있습니다.
21. 다음을 누르면 <스크립트 동작> 흑 병사를 `(6,2)`에 자동 배치합니다.
22. <설명창>특수 유닛은 완전히 포위될 때 능력을 발동합니다. 장군 돌을 선택하고 표시된 칸에 장군을 놓아보세요. 장군 선택 버튼을 하이라이트합니다.
23. 사용자가 장군 돌을 선택하면 장군을 놓을 `(5,2)`를 하이라이트합니다.
24. 사용자가 `(5,2)`에 장군을 놓으면 흑 병사를 `(5,3)`에 자동 배치해 포위를 완성하고 장군 캐릭터와 능력 효과를 재생합니다. 능력을 사용한 돌은 장군 문양을 유지하며 <설명창>장군이 인접한 적을 모두 제거하였습니다.

25. 다음을 누르면 <스크립트 동작> 백 병사를 `(3,2)`, 흑 병사를 `(4,2)` 순서로 자동 배치합니다.
26. <설명창>외교관 돌을 선택하고 표시된 칸에 외교관을 놓아보세요. 외교관 선택 버튼을 하이라이트합니다.
27. 사용자가 외교관 돌을 선택하면 외교관을 놓을 `(3,0)`을 하이라이트합니다.
28. 사용자가 `(3,0)`에 외교관을 놓으면 흑 병사를 `(2,1)`에 자동 배치해 백돌 그룹의 마지막 활로를 막고, 외교관 캐릭터와 능력 효과를 재생합니다. 능력을 사용한 돌은 외교관 문양을 유지합니다.

29. 다음을 누르면 <스크립트 동작> 백 병사를 `(4,3)`, 흑 병사를 `(6,3)` 순서로 자동 배치합니다.
30. <설명창>마법사 돌을 선택하고 표시된 칸에 마법사를 놓아보세요. 마법사 선택 버튼을 하이라이트합니다.
31. 사용자가 마법사 돌을 선택하면 마법사를 놓을 `(6,4)`를 하이라이트합니다.
32. 사용자가 `(6,4)`에 마법사를 놓으면 흑 병사를 `(7,4)`에 자동 배치해 포위를 완성하고 마법사 캐릭터와 능력 효과를 재생합니다.
33. 마법사 능력이 발동하면 <설명창>마법사가 인접한 적을 모두 제거하였습니다. 마법사는 능력이 발동한 후, 즉시 1회 순간이동할 수 있습니다. 표시된 빈칸으로 이동하세요. `(5,1)`만 하이라이트하고, 튜토리얼에서는 이동·이동 안 함 버튼을 표시하지 않습니다.
34. 사용자가 `(5,1)`로 이동하면 흑 왕 `(4,1)`과 연결된 흑 병사 `(4,2)`가 포획되어 백이 승리합니다. <설명창>튜토리얼 완료! 흑 왕을 포획하여 승리했습니다. 왕 배치, 포획, 성벽과 세 특수 유닛의 능력을 익혔습니다. 이제 AI 대전에서 삼류 고수에 도전해 보세요. 능력을 사용한 돌은 마법사 문양을 유지합니다. 같은 카드에 `시작하기`와 같은 스타일의 <나가기> 버튼을 표시하며, 누르면 로비로 돌아갑니다. 별도의 튜토리얼 완료 카드는 표시하지 않습니다.
