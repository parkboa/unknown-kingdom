# Explicitly keep existing releases in legacy mode until staged rollout is verified.
AUTH_MODE=required
SUPABASE_URL=https://ajmrlhfhrcsstauqgnip.supabase.co
SUPABASE_PUBLISHABLE_KEY=
# Dedicated login role inheriting daeguk_server; use pooler, TLS with certificate validation.
# Include sslmode=verify-full&sslrootcert=<URL-encoded absolute path to server/certs/prod-ca-2021.crt>.
# The certificate path must point to the file on the machine running the server.
DATABASE_URL=
AUTH_ALLOWED_ORIGINS=https://unknown-kingdom.vercel.app,capacitor://localhost
# Local HTTP cookie testing only. Do not set on deployed servers.
# AUTH_INSECURE_LOCALHOST=true
