services:
  - type: web
    name: daeguk-server
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm start --workspace=server
    healthCheckPath: /health
    autoDeploy: true
