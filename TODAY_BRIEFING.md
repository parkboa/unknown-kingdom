# 오늘의 작업 순서 브리핑

- 날짜: 2026-08-28
- 대상 저장소: `unknown-kingdom`

## 공동 편집 규칙

- 각 AI는 자신의 이름표가 붙은 구역에만 의견을 작성한다.
- 다른 AI의 의견은 읽고 참고할 수 있지만 수정, 삭제, 이동, 재정렬하지 않는다.
- 다른 AI에게 답할 내용이나 이견도 자신의 구역에 남긴다.
- 공통 작업 순서는 사용자가 최종 결정한다.
- 각 AI는 작업을 마친 뒤 `오늘의 작업 기록`에 자신의 이름표로 완료 내용과 검증 결과를 남긴다.
- 각 AI는 자신의 이름표가 붙은 작업 기록만 추가·수정할 수 있으며, 다른 AI의 기록은 수정하지 않는다.

## 공통 작업 순서

> 사용자 지시 (2026-08-28): ① 어제 만든 엔진·테스트와 초반 왕 보호 전략이 1·2·3단계에 미친 영향을 빠르게 점검한다. ② 한영 문구의 어색함·위치·차지하는 칸을 살피고 튜토리얼을 초심자도 이해하도록 친절하게 만든다. ③ `c047ab1`에 넣은 캐릭터 이미지를 왕처럼 트리거가 당겨질 때 나타나게 한다.
> 아래 5항목은 이 3항목을 실행 순서로 펼친 것이다.

1. **엔진 빠른 점검** — 어제 추가된 테스트와 왕 성벽 오프닝을 기준으로 현재 엔진 기준선을 확인하고, 변경된 초반 왕 보호 전략이 1·2·3단계의 판단과 시험 분포에 미친 영향을 짧게 정리한다. 오늘은 새 AI 정책을 바로 추가하기보다 기존 플래그의 유지·보류 판단과 다음 검증 대상을 확정한다.
2. **한영 디자인·문구 점검** — 한국어와 영어 문구를 화면별로 대조해 어색한 표현, 줄바꿈, 차지하는 칸과 위치를 확인한다. 모바일 폭에서 버튼·설명·팝업이 잘리거나 과도하게 넓어지지 않는지도 함께 점검한다.
3. **초심자 중심 튜토리얼 개선** — 게임을 처음 접한 사람도 왕 우선 배치, 성역, 특수 기물 발동과 선택 결과를 순서대로 이해하도록 설명을 더 친절하게 다듬고 Challenge 흐름으로 검증한다.
4. **캐릭터 아트 발동 연출 점검·연결** — 왕 도발 이미지의 현재 트리거를 기준으로 장군·외교관·마법사 흑백 이미지 6종을 각 특수 능력이 실제로 발동하는 시점에 표시하도록 설계·구현 범위를 확인한다. 로컬·PvE·온라인에서 같은 권위 이벤트를 사용하고, 연출이 중복되거나 턴 진행을 어긋나게 하지 않는 것을 완료 조건으로 삼는다.
5. **통합 검증** — 변경 범위에 맞는 엔진 테스트, Challenge 브라우저 테스트, 모바일 빌드와 한영 수동 화면 점검을 실행하고 결과를 오늘의 작업 기록에 남긴다.

### 역할 분담 (2026-08-28)

역할은 작업 맥락과 책임 범위로 나누되, 저장소 규칙에 따라 **읽기 전용 조사는 병렬**, **파일 수정은
한 번에 한 담당만 순차 진행**한다. 같은 파일을 함수·상수·CSS 선택자 단위로 구분하더라도 동시에
수정하지 않는다.

| 담당 | 주요 역할 | 책임 범위 | 완료 조건 |
| --- | --- | --- | --- |
| **CLAUDE CODE** | 1 엔진 점검 | `docs/AI_MODEL.md`, 시험 도구와 실험 결과를 중심으로 왕 성벽 오프닝이 1·2·3단계에 미친 영향 확인 | 운영 플래그 유지·보류 판단과 다음 검증 대상 정리 |
| **CODEX** | 2·3 한영 문구 + 초심자 튜토리얼 | `js/config.js`, `index.html`, 레이아웃 CSS, `TUTORIAL_STEPS`, 화면 스윕 도구 | 한영 문구·줄바꿈·점유 공간 개선 및 Challenge 흐름 검증 |
| **GOOGLE ANTIGRAVITY** | 4 캐릭터 아트 발동 연출 | 기존 특수 반응 이벤트 조사, `js/render.js`, 도발·연출 함수와 `.taunt-*` 계열 스타일 | 장군·외교관·마법사 흑백 이미지가 실제 발동 시 한 번만 표시되고 턴 동기화가 유지됨 |

배정 근거:

- 엔진은 어제 1·2·3단계 측정을 돌린 쪽이 맡는다. **넘기기 가장 비싼 맥락**이기 때문이다
  (`docs/AI_MODEL.md` 500줄 + 다섯 티어 설정 + 시험지 도구의 의미론). 반대로 아트 쪽 함정 3개는
  아래 `[CLAUDE CODE]` 구역에 이미 열 줄로 적어놨으므로 넘기기 싸다.
- 엔진 점검은 우선 운영 코드 변경 없이 끝낸다. 결함을 발견해 운영 코드 수정이 필요하면 바로
  고치지 않고 재현 조건과 수정 범위를 먼저 기록한 뒤 별도 작업으로 넘긴다.
- 아트 연출은 온라인 동기화에 닿을 수 있으므로 기존 권위 이벤트로 구현 가능한지 먼저 확인한다.
  기존 이벤트가 부족하다는 근거가 있을 때만 엔진 액션·이벤트·프로토콜 확장을 검토한다.

#### 실행 순서와 편집권

1. **병렬 사전 조사(읽기 전용)** — Claude는 엔진 영향표, Codex는 한영·튜토리얼 문제 목록,
   Antigravity는 기존 발동 이벤트와 이미지 연결 지도를 만든다.
2. **CLAUDE CODE 편집 차례** — 엔진 점검 결과와 시험 근거를 문서·도구 범위에 정리하고 커밋한 뒤
   편집권을 넘긴다.
3. **CODEX 편집 차례** — `js/config.js`의 한영 문구, `TUTORIAL_STEPS`, `index.html`과 레이아웃
   스타일을 수정하고 화면 스윕·Challenge 검증 후 별도 커밋한다.
4. **GOOGLE ANTIGRAVITY 편집 차례** — Codex 커밋 위에서 기존 권위 이벤트 재사용 여부를 확정한 뒤
   캐릭터 연출을 구현하고 별도 커밋한다.
5. **통합 검증** — 마지막 편집 담당이 전체 테스트와 모바일 빌드를 실행하고, 다른 담당은 읽기 전용
   리뷰로 자기 영역의 결과를 확인한다.

#### 충돌 방지 규칙

1. `js/config.js`의 한영 문구는 CODEX가 맡는다. 아트 연출에 새 문구가 필요하면 Antigravity가
   조사 단계에서 키와 문안을 요청하고, Codex 편집 차례에 반영한다.
2. 줄 번호는 앞선 수정으로 변하므로 소유 범위를 줄 번호로 고정하지 않는다. 튜토리얼은
   `TUTORIAL_STEPS`, 아트는 도발·연출 함수와 `.taunt-*` 선택자처럼 이름으로 식별한다.
3. `app.js`와 `styles.css`의 담당 영역이 논리적으로 달라도 동시에 수정하지 않는다.
4. 담당별 커밋과 검증 결과를 분리하고, 편집권을 넘기기 전에 작업 트리 상태를 공유한다.

#### 2·3번을 싸게 만드는 공용 도구

Playwright가 이미 `scripts/serve.mjs`(포트 4185)를 띄우고 뷰포트 393×852로 돈다. 여기에
**3폭(375 / 768 / 데스크톱) × 2언어 화면 스윕 스크립트**를 하나 추가하면 한영 점검이
"눈으로 훑기"에서 "PNG 비교"로 바뀐다. 2번 담당이 가장 먼저 만들고, 4번의 연출 확인에도
그대로 재사용한다.


## 오늘의 작업 기록

- 최신 기록을 아래에 추가한다. 형식: `[AI 이름] 완료한 작업 — 검증 또는 커밋`

### 2026-08-28

- [CODEX] 오늘 브리프를 엔진 영향 점검 → 한영 디자인·문구 → 초심자 튜토리얼 → 특수 캐릭터 발동 연출 → 통합 검증 순서로 갱신하고, 캐릭터 이미지 기록과 현재 미연결 상태를 확인했다 — `git diff --check` 통과.
- [CLAUDE CODE] 오늘 계획을 `[CLAUDE CODE]` 구역에 적고, `오늘의 작업 기록`에서 2026-08-27 보존 기록을 제거했다(실질은 `docs/AI_MODEL.md`와 git 히스토리에 남음) — 기준선 `npm test` 132개 전부 통과(웹 12 · 엔진 116 · 서버 4).
- [CLAUDE CODE] 사용자 요청에 따라 `공통 작업 순서` 아래에 `역할 분담 (2026-08-28)`을 넣었다 — 파일 소유권 기준 3자 배정, 충돌 규칙 4개(`js/config.js` 단독 소유 · `app.js`/`styles.css` 구역 분리 · 커밋 분리), 병렬 가능 조건, 한영 점검용 화면 스윕 도구 제안. `git diff --check` 통과.
- [CODEX] 사용자 결정에 따라 역할 분담을 “읽기 전용 조사는 병렬, 파일 수정은 Claude → Codex → Antigravity 순차”로 정정했다 — 줄 번호 대신 이름 기반 책임 범위, 기존 권위 이벤트 우선 재사용, 담당별 커밋·편집권 인계 규칙을 명시하고 `git diff --check` 통과.
- [CODEX] 한영 UI·초심자 튜토리얼을 개선하고 375·768·1280px × 한영 2개 언어의 로비·AI 설정·설정·챌린지·튜토리얼 화면 36장을 생성하는 Playwright 스윕을 추가했다 — 가로 넘침·문구 잘림·잠금 레이블 현지화를 자동 검사하며 전체 132개 테스트, 브라우저 7개, 모바일 빌드와 `git diff --check` 통과. 커밋 `2396f43`; 다음 편집권은 캐릭터 아트 연출 담당에게 인계한다.
- [CLAUDE CODE] 공통 1(엔진 점검)을 읽기 전용 분석으로 마쳤다 — 운영 코드 0줄 변경. **1·2단계 시험지(`ai-tactics-suite.mjs`)에 어제 3단계에서 고친 표집 결함이 그대로 남아 있음을 확인했다**: 첫 6플라이가 균등 무작위이고 규칙상 첫 배치가 왕이라 왕이 항상 무작위로 놓이는데, 배치 3·4번째는 `openingWallStones` 링 계획을 타서 '무작위 왕 + 교리적 성벽'이라는 잡종 분포가 된다. 재생성 코퍼스(seed 20260827, 8게임, 29문제 × 5티어)에서 어제 1a·1c 보류 근거였던 마스터 −1 회귀는 재현되지 않았고 오히려 +2/−0이었으며, 1c는 145결정 중 18개를 전문·마스터에만 바꾸고 손실 0, 1a는 하위 티어까지 새며 전문 1개 상실, 1b는 0/145로 두 코퍼스 연속 완전 무효였다. 2단계는 `probe-objectives` 출력이 `generatedAt`만 빼고 어제와 동일(판정 불변), 3단계는 시험지가 이미 교리 오프닝을 써서 재측정 불필요. **결론: 1a·1b·1c·2단계 전부 OFF 유지하되 근거를 '마스터 회귀'에서 '시험 해상도 부족'으로 교체하고, 다음 검증 대상은 정책이 아니라 시험지 표집이다.** 상세는 `[CLAUDE CODE]` 구역. `docs/AI_MODEL.md` 반영과 표집 수정에는 편집권이 필요하다 — 아트 작업 뒤를 요청한다.
- [CLAUDE CODE] 편집 차례를 받아 1·2단계 시험지를 고치고 재측정했다 — `ai-tactics-suite.mjs`의 표집을 `probe-special-doctrine.mjs`와 같은 모양으로 맞췄다(교리가 첫 5배치, `--opening 6` → `--random-plies 3`). 수용 시험: 왕 성벽 거리가 흑 4·백 1로 모두 티어 밴드 안(수정 전 흑 {4,5,6}·백 {1,3}). **고친 시험지에서 판정의 부호가 뒤집혔다** — 마스터 기준선 `true_atari` 6/12 → 11/12, 1a는 +2가 아니라 전문·마스터 각 −1(회귀 확인), 1c는 +2가 아니라 정확히 중립(수 10개 변경, 정답 0개 변경), 1b는 세 코퍼스 연속 0. 결론은 08-27과 같은 **전부 OFF 유지**지만 근거가 올바른 분포로 교체됐다. `probe-objective-model-tactics.mjs`에 `--output`을 추가해 읽기 전용 분석이 추적 파일을 더럽히지 않게 했다. `docs/AI_MODEL.md`에 상태표·`남은 문제`·신규 절을 반영. 검증: `npm test` 132개 전부 통과, Grandmaster 전술 5/5, `git diff --check` 통과. 커밋 `024ea80`. 다음 편집권은 캐릭터 아트 담당에게 넘긴다.
- [CLAUDE CODE] 리그 실행을 보류하고 `docs/AI_FLAG_STRUCTURE.md`를 새로 썼다 — **플래그들이 독립 축이 아님을 실험으로 증명했다**: 1c 단독 ≡ `kingTacticalPriority: 0`(0/140 결정 차이), 2단계 단독 ≡ 무효(0/140, 1a 없이는 `kingDangerValue` 미호출), 1b 단독 ≡ 무효(0/140, 지수 12 × 블렌드 0.1로 두 겹 억제), 대조군 1a는 4/140으로 비교 방법 작동 확인. 버릴 것 4개(`ironcladKingDefense`는 마스터에 `true`인데 읽는 곳 0곳, `terminalObjectiveModel`은 1a·1b·1c 별칭, `specialAnchorThreat` 최상위 노출, `searchAlgorithm` 티어 노출)와 축을 A(측정: 1a+2단계)·B(결합: 1b+1c) 둘로 줄이는 안, 그리고 **4단계 교리와 출시 평가함수가 같은 왕을 −8과 0으로 반대로 읽는 미해결 모순**을 정리했다. 운영 코드 변경 0줄, 문서 1개 신규. 다른 에이전트 의견 대기. 부수 확인: 기존 티어 리그 60판은 **네 플래그가 전부 꺼진 기본 설정**으로 돌았고(`commonScoreProfile: null`, `scoreAblations: []`), 켜고 돌린 적이 없다.
- [CLAUDE CODE] `docs/AI_FLAG_STRUCTURE.md`의 "4단계 모순" 절을 **철회하고 정정했다** — 사용자가 "밴드는 티어별로 다르지 않냐"고 지적해 실측했더니 내 주장 두 곳이 틀렸다. (1) 밴드는 허가라 초급 0~6·중급 0~5·고급 0~4가 모두 중앙 4:4를 포함하고, **그 세 티어는 교리 전과 같은 자리에 왕을 둔다** — 실제로 성벽으로 움직인 건 전문(3:4)·마스터(1:4) 둘뿐. (2) 교리 오프닝의 왕 활로는 **7~8이지 1이 아니다** — 왕을 자기 돌 4개로 감싸면 그룹이 커져 활로가 늘어난다. `docs/AI_MODEL.md`의 −8은 `kingLibertyCount == 1`인 종반 예시인데 오프닝에 잘못 갖다 붙였다. **모순은 없다.** 남는 질문은 난이도 설계 의도로 바꿔 적었다 — 하위 세 티어에서 밴드가 비구속적인 것과 `openingWallStones: 4`가 다섯 티어 동일한 것이 의도인가. 나머지 결론(축을 A·B 둘로, 버릴 것 4개)은 실험 근거가 그대로라 유지. 운영 코드 변경 0줄, `npm test` 132개 통과. 커밋 `10b73d7`.
- [CLAUDE CODE] 사용자가 설계 의도를 밝혀 `docs/AI_FLAG_STRUCTURE.md`를 **보정 스택 틀로 다시 썼다** — 초급은 날것, 상위로 갈수록 튜닝, 튜닝이 만든 겁쟁이를 다음 플래그가 상쇄. 코드가 그대로다: 믿음 세계가 1→6으로 뛰는 고급에서 `instantWinRiskTolerance 0.7`이 정확히 켜진다. 거울(`specialAssaultTargets`)은 이미 구현돼 전문·마스터에 켜져 있어 `docs/AI_MODEL.md`의 "코드에 전혀 없다"를 갱신했다. **사다리가 끊긴 곳 셋**: `openingWallStones: 4`가 다섯 티어 동일해 초급이 날것이 아님(실측 첫 다섯 수가 교과서적 완전 링), `ironcladKingDefense`는 기획됐다 배선 빠진 보정으로 보여 삭제 제안 철회, `breach_square` 겁쟁이는 아직 짝이 되는 보정 없음. **그리고 오늘 내 측정의 절반이 잘못된 질문이었다** — `--tier-override`가 다섯 티어 전부에 적용되므로 사다리가 아니라 평평한 판을 쟀다. 퇴화 증거는 무관하나 1a의 부호는 다시 물어야 한다. 제안 순서를 '측정 도구에 티어 범위 추가 → `openingWallStones` 사다리화 → 정리 → A·B 축 → 티어 범위와 함께 측정 → 리그'로 교체. 운영 코드 변경 0줄, `npm test` 132개 통과. 커밋 `bb85c44`.
- [CLAUDE CODE] 사용자 결정에 따라 `openingWallStones`를 티어별 사다리로 갈랐다 — 초급 1 · 중급 2 · 고급 2 · 전문 3 · 마스터 4 (`js/ai.js`). 이전에는 다섯 티어 모두 4라서 **밴드가 만든 사다리를 상쇄**하고 있었다 — 완전 링은 어디에 서든 완전 링이므로 초급이 마스터와 같은 교과서적 모양으로 열었고, `variance`는 `openingWallPlan`이 후보를 링으로 필터한 뒤라 순서만 흔들 뿐이었다. 적용 후 실측: 초급 `king@4:4 s@3:4 s@5:4 s@2:4 s@6:4`(링 1돌만 강제, 이후 자기 판단), 마스터는 완전 링 유지. **오늘 첫 운영 코드 변경이다.** 두 시험 스크립트의 `DOCTRINE_DEPLOYMENTS` 주석도 가장 긴 교리(마스터 4돌) 기준임을 밝히도록 고쳤다. 검증: `npm test` 132개 전부 통과, Grandmaster 전술 5/5, 브라우저 7개 통과(Codex의 화면 스윕 포함), `git diff --check` 통과. 커밋 `a8c089c`. **주의: 오프닝이 또 바뀌었으므로 오늘 낸 1단계 수치는 다시 낡았다** — 다만 `--tier-override`가 다섯 티어 전부에 적용되는 문제가 먼저라 재측정은 그 도구 수정 뒤로 미룬다.
- [CODEX] 사용자에게서 최종 원칙을 확인해 AI 플래그 작업을 이어받았다 — 목표는 결정론적 티어 간 승률이 아니라 **각 티어의 플레이 정체성**이며, 플래그는 검증 후 티어 로직에 흡수하고 제거하는 임시 실험 장치다. 두 시험 스크립트에 `--override-tiers`를 추가해 일부 티어에만 보정을 적용하고 나머지를 대조군으로 보존했으며, 공용 파서와 4개 단위 시험을 추가했다. 문서에는 교리·혼합 추천·제한된 실수·실제 PvE 저널의 층별 검증안을 기록했다. `npm test` 136개 전부 통과(웹 16 · 엔진 116 · 서버 4), `git diff --check` 통과.
- [CODEX] 사람형 대리 분포의 첫 층인 `mixed-tier` 표집을 구현했다 — 교리 이후 같은 국면에서 다섯 티어가 추천한 수를 모아 중복 제거 후 seed로 선택하며, 기존 균등 무작위는 재현 호환용 `uniform` 기본값으로 남긴다. 두 시험 보고서에 `perturbationPolicy`를 기록하고, 특수 교리 시험의 override가 코퍼스 생성에 먼저 유입되던 누수도 차단했다. 실제 CLI 스모크에서 `breach_square` 1개를 생성했고 scoped override 뒤에도 초급·중급 선택 불변을 확인했다. `npm test` 139개 전부 통과(웹 19 · 엔진 116 · 서버 4), 두 스크립트 구문 검사와 `git diff --check` 통과.
- [CODEX] 실제 사람 대 AI 기록을 최우선 시험 층으로 쓰는 `--pve-journals` 입력을 두 시험기에 추가했다 — 브라우저 JSONL을 권위 엔진으로 전체 검증한 뒤 사람의 수와 특수 반응이 끝나 AI 차례가 된 조용한 상태만 추출한다. 파일 또는 다운로드 폴더를 받고, 사람·AI 진영 메타데이터를 추측하지 않으며, 각 답안에 파일명·게임 ID·행동 인덱스·당시 티어를 보존한다. 실제 저널을 합성 `soldiers-only`나 perturbation 옵션과 섞는 잘못된 명령도 거부한다. 파일·폴더 로더와 실제 CLI 보고서 출처를 자동 시험한다. `npm test` 143개 전부 통과(웹 23 · 엔진 116 · 서버 4), 두 스크립트 구문 검사와 `git diff --check` 통과.
- [CODEX] 사용자 요청으로 모든 목적함수 플래그 ON/OFF 티어 리그를 조합당 색상 교대 2판씩 실행했다(seed 20260828, 조건당 20판) — 동일 매치업 11/20의 승자가 바뀌어 묶음의 행동 영향은 컸다. ON/OFF 평균 62.2/52.1수, 영토 종료 8/4판, capped 0. 영토전 중 1~3돌 차이의 사실상 동점은 ON 3판(41–40, 41–39, 40–41), OFF 0판이었다. 초급은 OFF 4/8에서 ON 0/8로 내려가 모든 보정을 날것 티어에 일괄 적용하면 정체성을 훼손한다는 경고를 남겼다. 전체 결과는 `experiments/objective-flags-two-game-league.json`에 저장했다. `npm test` 143개 전부 통과, 토너먼트 구문 검사·결과 JSON 재계산·`git diff --check` 통과.
- [CODEX] 사용자 결정에 따라 AI 카탈로그 정리의 첫 경계를 구현했다 — 왕 첫 배치와 성역 기간을 `opening_king`·`opening_sanctuary`·`opening_free_play`로 분리하고, 성역 종료 뒤에만 `catalog` 단계로 넘어가는 계약을 추가했다. 기존 티어별 성벽 거리와 왕 주변 돌 1·2·2·3·4는 행동 그대로 최상위 후보 제약으로 유지하며, 확정 승리와 왕 단수 구조도 별도 함수로 분리했다. 다섯 티어 계약 시험, Grandmaster 전술 5/5, 전체 144개 테스트(웹 23 · 엔진 117 · 서버 4), 구문 검사와 `git diff --check` 통과.
- [CODEX] 성역 계약 아래의 후보를 `king`·`special`·`board` 카탈로그로 관찰 분류했다 — 후보는 중복 계산을 막기 위한 주 카탈로그 하나와 교차 신호를 함께 가지며, `candidate_collection`부터 탐색·숨은 위험·예비 후보·최종 선택까지 `kept`·`stopped`·`vetoed` 이유를 기록한다. 선택 수에는 항상 자기 경로가 남고, 진단 옵션 및 토너먼트 `--trace-games`에서는 모든 후보 기록을 보존한다. 분류와 선택 순서는 아직 연결하지 않아 점수·승패는 바꾸지 않았다. 전체 147개 테스트(웹 23 · 엔진 120 · 서버 4), 세 파일 구문 검사와 `git diff --check` 통과.
- [CODEX] 성역 종료 뒤 152결정을 실제 추적해 카탈로그 탈락 지점을 분리했다 — `special`은 `search_pool` 생존율 11.6%(Grandmaster 3.7%)로 가장 먼저 고갈됐고, Grandmaster 특수기물 830개 중 799개는 `midgame_special_attack_filter`가 제거했다. `board`는 중급·고급·전문에서 탐색 후 `risk_pool` 생존율이 5.5%·3.3%·3.3%로 다시 급락했으며 원인은 전역 후보 수 제한이었다. 숨은 위험 veto는 전체 2건뿐이었다. 후보 수집의 15.5%인 `king`이 최종 선택의 61.8%가 되는 구조적 편향을 확인했지만, 아직 보존량·점수·선택은 바꾸지 않았다. 원본은 `experiments/ai-catalog-survival.json`. 전체 148개 테스트(웹 23 · 엔진 121 · 서버 4), 관련 구문 검사와 `git diff --check` 통과.
- [CODEX] 사용자 설명에 따라 특수기물을 ‘후반 돌파’가 아닌 ‘잠금 해제 직후 초반 매설’로 다시 추적했다 — 12개 플레이어 런 모두 첫 특수를 자기 6~7번째 배치에 심었고, Grandmaster는 두 경기 모두 세 특수를 정확히 6·7·8번째에 연속 배치했다. 전체 34개 특수 배치는 모두 적 기물과 접촉했고 31개는 적 왕 그룹 활로였다. 따라서 낮은 후보 생존율을 특수 채널 고갈로 해석한 앞 기록은 철회한다. Grandmaster 필터는 실제 선택을 억제하지 않고 수백 좌표를 공격 후보 몇 개로 좁혔다. 다음 질문은 사용 시점이 아니라 티어별 매설 위치 품질 차이다. 원본은 `experiments/ai-special-planting.json`; 점수·선택 변경 없음. 전체 149개 테스트(웹 24 · 엔진 121 · 서버 4), 관련 구문 검사와 `git diff --check` 통과.
- [CODEX] 특수기물 티어 계약 1단계를 구현했다 — 첫 사용은 초급 9+, 중급 7~8, 고급·전문·Grandmaster 6~8번째 자기 배치로 나누고, 최대 연속 수를 1·1·2·3·3으로 제한했다. 특수 포함 단수 무리는 초급~고급이 병사로 구조하고 전문·Grandmaster는 구조 후보를 제거해 희생을 허용한다. 확정 왕 포획과 자기 왕 단수 구조는 계약보다 우선한다. 동일 6경기 대표 수순이 초급 9·12·15, 중급 7·9·11, 고급 6·7·9, 전문·Grandmaster 6·7·8로 갈렸고, 집중 시험에서도 구조/희생 선택이 다섯 티어 계약대로 분리됐다. Grandmaster의 발동 후 후속 매설은 2단계로 남겼다. `experiments/ai-special-planting.json` 갱신. 전체 154개 테스트(웹 24 · 엔진 126 · 서버 4), 관련 구문 검사와 `git diff --check` 통과.
- [CODEX] Grandmaster 특수기물 계약 2단계를 구현했다 — 특수 2개를 쓴 뒤 하나가 발동하고 재고 1개가 남았을 때, 장군·마법사가 실제로 비운 칸과 마법사 이동 원점을 다음 자기 배치까지 후속 매설 후보로 보존한다. 상대가 다시 차지한 칸, 외교관 전환, 전문 이하에는 적용하지 않는다. 후보는 `midgame_special_attack` 탐색에 남기되 강제하지 않아, 적 왕 앞의 유효 빈칸에는 남은 특수를 심고 탐색값이 나쁜 빈칸은 일반 수를 선택하는 두 집중 시험이 모두 통과했다. 전체 157개 테스트(웹 24 · 엔진 129 · 서버 4), 관련 구문 검사와 `git diff --check` 통과.
- [CODEX] Vercel 프로덕션에 노출된 `연출 바로보기`를 개발자 모드 전용으로 제한했다 — HTML 단계에서 기본 숨김 처리하고 `DEVELOPER_MODE`일 때만 다시 표시해, 실제 장군·외교관·마법사 컷신 동작은 유지하면서 일반 접속의 내부 시험 도구만 제거했다. 전용 회귀 시험을 추가했고 전체 161개 테스트(웹 28 · 엔진 129 · 서버 4), `app.js` 구문 검사와 `git diff --check` 통과.
- [GOOGLE ANTIGRAVITY] 특수 캐릭터 발동 연출(장군·외교관·마법사 흑백 컷씬 6종) 연결 및 **장군 스킬 연출 시퀀스(컷인 “감히 나를 막아” 0.7초 → 상하좌우 흰색 칼날 선 번쩍임 + 대상 돌 검은 절단선 3~5px 분리/페이드아웃 0.5초 → 결과 반영)**를 완성했다 — 엔진의 `special_activated` 및 `piece_removed(general_reaction)` 이벤트를 3단계 연출 시퀀스(`cutin` → `slash` → `done`)로 처리하고, 로컬·PvE·온라인 전 모드 연동 및 3개 전용 단위 시험 통과(`test/special-unit-cutscenes.test.js`). 전체 159개 테스트(웹 26 · 엔진 129 · 서버 4), 모바일 빌드와 `git diff --check` 통과.

### 2026-08-31

- [CODEX] 튜토리얼 작업에서 본게임으로 번진 치명적 회귀를 복구했다 — `resetGame()`의 미선언 튜토리얼 상태를 제거하고 시작 버튼 클릭을 취소하던 CSS 및 끊어진 규칙 경계를 복원했다. PvE 흑·백 진영의 선택 성벽 하단 배치, 결과 화면의 다시 하기·로비 복귀, 튜토리얼 4페이지 인트로 종료·오버레이 제거·E8 왕 배치를 브라우저 회귀 시험으로 고정했으며 모바일 영어 성역 문구 잘림도 정리했다. `npm test` 161개, 핵심 브라우저 5개, 3폭×2언어 시각 검사 6개, 모바일 빌드와 `git diff --check` 통과.
- [CODEX] 새 튜토리얼 스크립트 1~12번을 0 기반 `(x,y)` 좌표로 정리하고, 백 왕 `(4,7)` 배치부터 왕 주변 병사·성벽용 흑돌·포획 구도까지 한 대국 안에서 권위 엔진 착수로 자동 진행되게 연결했다. 사용자 입력은 왕과 마지막 `(4,4)` 포획 수만 허용하며, 자동 착수 중 입력·다음 버튼을 잠갔다. 지정된 모든 좌표, `(4,4)` 하이라이트, `(4,5)` 흑돌의 백 점령 전환과 기존 후속 튜토리얼 완료를 브라우저 시험으로 고정했다 — 전체 161개 테스트, 튜토리얼 브라우저 시험 1개, 3폭×2언어 시각 검사 6개와 `git diff --check` 통과.
- [CODEX] 튜토리얼의 실제 성역 규칙과 설명용 표시를 분리했다 — 왕 주변 자동 병사 4개가 정상 착수 횟수로 집계되어도 첫 단계의 `다음`을 누르기 전까지 양쪽 3×3 성역 표시를 유지하고, 다음 단계 진입 시 표시만 제거한다. 인트로 설명창은 첫 대화부터 보이되 안내 문구와 `시작하기` 버튼은 “흑돌이 선공…” 대화 이후에만 나타나게 했으며, 버튼의 `hidden` 속성을 덮던 CSS도 바로잡았다. 전체 161개 테스트, 튜토리얼 브라우저 시험 1개, 3폭×2언어 시각 검사 6개 통과.
- [CODEX] 튜토리얼 `시작하기` 버튼을 공통 주요 버튼의 28px 소형 변형으로 정리했다 — 금색 테두리·배경·글꼴·hover 규칙을 맞추고 flex 양축 정렬과 line-height를 명시해 텍스트를 버튼 중앙에 배치했다. 튜토리얼 브라우저 시험 1개와 3폭×2언어 시각 검사 6개 통과.
- [CODEX] 성역 설명을 두 카드로 분리했다 — 왕 배치 직후에는 왕과 주변 8칸 설명과 실제 활성 성역만 보여주고, 첫 `다음`에서 보호 기간 설명으로 전환하며 양쪽 왕 주변 병사 8개의 자동 착수를 시작한다. 마지막 자동 수에서 엔진 규칙대로 성역이 사라지므로 앞서 만든 강제 표시 상태를 제거했다. 한영 문구와 임시 스크립트를 함께 갱신하고 첫 카드의 왕 2개·성역 18칸, 두 번째 카드의 자동 병사 8개·성역 해제를 브라우저 시험으로 고정했다 — 전체 161개 테스트, 튜토리얼 브라우저 시험 1개, 3폭×2언어 시각 검사 6개 통과.
- [CODEX] 포획 설명의 자동 착수 순서를 포획 대상 흑돌 `(4,5)` → 백돌 `(3,5)` → 흑 진영 성벽 흑돌 `(1,0)` → 백돌 `(5,5)` → 백 진영 성벽 흑돌 `(1,8)`로 조정해, 가운데 포획 대상을 먼저 보여준 뒤 양쪽 성벽용 흑돌이 놓이게 했다 — 튜토리얼 전체 브라우저 시험 통과.
- [CODEX] 포획 다음의 성벽 수업 두 장면을 기존 대국에 이어 구현했다 — 첫 장면은 자동 5수 뒤 `(2,0)` 백 착수로 흑돌 `(1,0)`이 자기 성벽 활로를 얻어 생존하고, 두 번째 장면은 자동 5수 뒤 `(2,8)` 백 착수로 흑돌 `(1,8)`이 상대 성벽에 막혀 백 점령돌로 전환된다. 두 스크린샷의 자동 배치 10개 좌표, 하이라이트, 안내·결과 문구, 생존·포획 결과와 이후 특수 유닛 수업까지 브라우저 시험으로 고정했다 — 전체 161개 테스트와 튜토리얼 브라우저 시험 통과.
- [CODEX] 자동 착수 중 `다음` 버튼이 숨겨져도 `tutorial-actions` 영역의 높이를 32px로 유지해 설명 카드의 문구가 위아래로 이동하지 않게 했다 — 계산 높이와 최소 높이를 브라우저 회귀 시험으로 고정했고 튜토리얼 전체 시험 통과.
- [CODEX] 튜토리얼 안내문 행의 수직 중앙 정렬을 제거해 한 줄과 여러 줄 문장이 항상 같은 상단 위치에서 시작하게 했다 — 한 줄 왕 배치 안내와 여러 줄 성역 안내의 실제 브라우저 Y 좌표가 같은지 회귀 시험으로 고정했다.
- [CODEX] 상대의 마지막 착수를 따라가는 노란 사각형을 튜토리얼에서만 숨겼다 — 본게임의 추적 표시와 모든 모드의 돌 배치 애니메이션은 유지하며, 표시 판정 단위 시험과 자동 착수 이후 튜토리얼 보드 시험으로 경계를 고정했다.
- [CODEX] 튜토리얼 두 번째 인트로부터 쓰는 왕 대치 이미지만 기존 `1.2` 배율에서 정확히 10% 큰 `1.32`로 확대했다 — 첫 번째 병사 안내 이미지와 본게임 컷신 크기는 유지했다.
- [CODEX] 튜토리얼 인트로 카드가 페이지마다 아래에서 8px 상승하며 나타나던 0.3초 페이드를 제거했다 — 카드는 즉시 고정 위치에 표시하고, 다음 화살표의 1.1초 상하 반복 애니메이션은 그대로 유지했다.
- [CODEX] 상대 성벽 포획 결과의 한국어 안내를 “흑 병사는 백의성벽에 막혀 포획되었습니다.”로 구체화하고 임시 튜토리얼 스크립트와 브라우저 기대 문구를 함께 갱신했다.
- [CODEX] 성벽 수업 뒤 장군 실습을 연속 대국 위에 재구성했다 — 문구를 “백의 성벽”으로 바로잡고 특수 유닛 5턴 소개 카드를 추가했다. 다음을 누르면 흑 병사 `(6,2)`를 자동 배치하고 장군 버튼을 먼저 강조하며, 사용자가 장군을 선택한 뒤에만 `(5,2)`를 강조한다. 장군 배치 후 흑이 `(5,3)`으로 포위를 완성해 장군 캐릭터 컷인과 참격 효과를 실행한다. 임시 스크립트와 전체 브라우저 흐름을 함께 갱신했다.
- [CODEX] 특수 유닛 소개 문구를 “특수 유닛을 배워 봅시다. 대국 시작 후 자신의 돌을 다섯 번 놓으면 특수 유닛을 사용할 수 있습니다.”로 다듬어 맞춤법과 실제 해금 조건을 명확히 했다.
- [CODEX] 장군 실습 안내에서 뒤이어 자동으로 보이는 흑의 포위 설명을 미리 반복하던 문장을 제거해, 능력 발동 조건과 장군 선택·배치 지시만 남겼다.
- [CODEX] 능력을 사용한 특수 유닛이 일반 병사 문양으로 바뀌지 않고 공개된 원래 문양을 유지하게 했다 — 엔진의 `type=soldier`·`abilityUsed=true` 판정은 그대로 두어 포획·활로·영토·AI에는 영향을 주지 않으며, 공개되지 않은 전환 기물은 원래 정체를 노출하지 않는다. 장군·외교관·마법사 결과 문구에서 일반 병사 전환 설명을 제거하고 장군 문구를 “장군이 인접한 적을 모두 제거하였습니다.”로 변경했다.
- [CODEX] 마지막 수 좌표가 남아 있다는 이유로 단순 재렌더링마다 돌 배치 애니메이션이 반복되던 구조를 제거했다 — 실제 `piece_deployed` 이벤트 때만 한 번 쓰는 애니메이션 신호를 전달하고 즉시 폐기하며, 온라인 상태 수신은 기물 ID까지 비교해 재배치도 구분한다. 장군 선택 시 `(6,2)` 흑돌은 다시 튀지 않고 실제 장군 배치만 움직이는지 브라우저 시험으로 고정했다.
- [CODEX] 아래 인트로 대화창의 수직 중앙 정렬을 안내 카드에 한해 상단 정렬로 바꿔 한 줄과 두 줄 문장의 첫 줄이 같은 높이에서 시작하게 했다 — 첫 두 줄 카드와 마지막 한 줄 카드의 실제 브라우저 Y 좌표를 비교해 고정했다.
- [CODEX] 장군 수업 다음 외교관 실습도 같은 연속 대국 위에 연결했다 — 다음을 누르면 흑돌 `(4,2)`를 자동 배치하고 외교관 버튼을 먼저 강조하며, 선택 뒤에만 `(3,0)`을 강조한다. 외교관 배치 후 흑이 백돌 그룹의 마지막 활로 `(2,1)`을 막아 포획을 만들고, 외교관 컷인·금화 전환 효과로 `(4,0)`·`(3,1)` 흑돌을 백돌로 전환한다. 사용 완료 외교관 문양 유지와 결과 문구까지 전체 브라우저 시험으로 고정했다.
- [CODEX] 특수 유닛 실습의 실제 흑백 교대를 복구했다 — 장군 발동 뒤 `백 (3,2) → 흑 (4,2)`, 외교관 발동 뒤 `백 (4,3) → 흑 (6,3)` 보조수를 자동 배치한 다음 사용자가 각각 외교관과 마법사를 두게 한다. 마법사는 `(6,4)`에서 흑 `(7,4)`의 포위로 컷인·룬 효과를 발동하고, 일반 이동·이동 안 함 버튼 없이 `(5,1)` 한 칸만 강조·허용한다. 그곳으로 이동하면 흑 왕 `(4,1)`과 흑 병사 `(4,2)`를 함께 포획해 백이 승리하며, 사용 완료 마법사 문양을 유지하는 전체 흐름을 브라우저 시험으로 고정했다.
- [CLAUDE CODE] 대국 종료 알림 배너를 다시 배치했다 — 승리 돌(44px)과 `흑 승리`(15px)를 오른쪽 `경기 결과`/`다시 대국` 프레임과 같은 92px×전체 높이 블록으로 왼쪽에 세우고(알림 상태의 `.tutorial-guide-content`를 `align-self: stretch`로 채워 좌우 높이 일치), 가운데는 `대국 종료`(최대 20px 굵게) 아래 **종료 사유**를 설명 폰트(`--font-size-body`)로 두 줄 구성으로 바꿨다. 사유는 남은 유닛 수(`matchEndScore`)를 대체한 `localizeResultReason(state.resultReason)`이다. 길이가 언어마다 달라 `fitMatchResultVerdict()`로 제목은 한 줄에 들어갈 때까지(최소 13px), 사유는 3줄 안에 들어갈 때까지(최소 9px) 1px씩 줄이고 창 크기 변경 시 재계산하며, 한국어는 `word-break: keep-all`로 어절 단위로 끊는다. 요소를 `#matchResultHeadline`·`#matchResultReason`으로 나누면서 옛 `#matchResultScore`를 없앴고, 브라우저 시험의 '점수·돌 중앙 정렬' 검사를 좌우 배치·좌우 블록 크기 일치·두 줄 순서·글자 크기 위계 검사로 교체했다. 참조가 0곳이 된 `js/config.js`의 `matchEndScore`(en·ko)는 사용자 확인을 받고 지웠다 — 영역 승부의 `보드 종료: …`·`더 이상 둘 수 없어 종료: …`는 `boardFilled`·`noLegalMoves`라는 별개 키이고, 남은 유닛 수는 `경기 결과` 모달의 `흑/백 유닛` 항목에 그대로 있다. 알림 안의 `경기 결과`·`다시 대국` 버튼은 금색 강조(테두리·글자·따뜻한 그라디언트)를 빼고 중립 회색 테두리와 옅은 배경으로 낮췄다 — PvP 재대국 제안 상태(`#playAgainBtn.rematch-offered`)의 금색 강조는 기능 신호라 그대로 둔다. 검증: `npm test` 164개(웹 31 · 엔진 129 · 서버 4)와 브라우저 시험 11개 통과.
- [CLAUDE CODE] `경기 결과` 카드의 순서와 항목을 정리했다 — `총 배치 수` → `종료 방식` → `흑/백 유닛` → `흑/백 포획` 순으로 바꾸고, `흑/백 특수 사용`(`#resultRedSpecials`·`#resultBlueSpecials`)과 `포획 수는 대국 기록…` 안내문을 삭제했다. 딸린 `.captures-score-note` 스타일과 `js/config.js`의 `redSkills`·`blueSkills`·`capturesNotScored`(en·ko)도 함께 지웠다. 엔진의 `state.stats.specialsUsed` 집계는 그대로 두어 기록은 남는다. 브라우저 시험에 항목 순서(`총 배치 수`·`종료 방식`)와 유닛·포획 블록의 세로 순서, 삭제한 요소의 부재 검사를 넣었다. 검증: `npm test` 164개(웹 31 · 엔진 129 · 서버 4)와 브라우저 시험 11개 통과.
- [CLAUDE CODE] 특수 유닛 발동 확인 카드(`#pendingSpecialModal`)를 없애고 자동 발동으로 바꿨다 — 캐릭터 컷인이 `special_activated` 이벤트로 양쪽 화면에 나오는 지금, 카드는 거절 선택지가 없는 강제 탭이었다(튜토리얼은 이미 카드를 숨기고 스크립트가, PvE의 AI는 `AI_SPECIAL_REVEAL_DELAY_MS` 뒤에 자동 발동해 왔다). `scheduleSpecialAutoActivation()`이 포위 직후 `SPECIAL_AUTO_ACTIVATE_MS`(900ms) 뒤에 발동시키고 — PvP는 소유자 클라이언트만 전송, PvE는 사람 쪽만(AI는 기존 경로), 퍼즐·로컬은 소유자 무관 — 도발 연출 중이거나 판이 끝났으면 예약을 취소한다. 마크업·`.special-notice-card`/`.help-card` 스타일·클릭 핸들러와 `js/config.js`의 `activateSpecialTitle`·`activateSpecialPrompt`·`activateSpecialButton`·`waitingSpecialPrompt`·`specialUnit`(en·ko)을 지웠다(`TODAY_BRIEFING.md` 문구 점검 목록의 `activateSpecialPrompt`/`activateSpecialButton` 항목은 이제 대상이 없다 — 다른 AI 구역이라 손대지 않았다). 검증용으로 `?demo=special-pending`(포위된 흑 장군)을 추가하고, 카드 부재 → 장군 컷인 → 인접 백돌 4개 제거까지 확인하는 브라우저 시험을 넣었다. 검증: `npm test` 164개(웹 31 · 엔진 129 · 서버 4)와 브라우저 시험 12개 통과.
- [CLAUDE CODE] 사용자 요청으로 특수 발동 규칙을 모드 하나로 합쳤다 — 예전에는 같은 순간에 세 경로·세 상수가 따로 있었다(튜토리얼 스크립트 1600ms, PvE의 AI `scheduleAiTurn` 1500ms, 나머지 900ms). 이제 `SPECIAL_ACTIVATE_DELAY_MS`(1200ms) 하나로 `scheduleSpecialAutoActivation()`이 전부 처리한다. 규칙: **포위된 특수는 1200ms 뒤 스스로 발동하고, 실행 주체는 그 진영을 조종하는 클라이언트다** — PvP만 소유자 본인으로 제한하고(`networkSession.ready` 필요), 튜토리얼·PvE·퍼즐·로컬은 한 화면이 양쪽을 조종하므로 소유자를 가리지 않는다. `scheduleAiTurn`의 `pendingSpecial` 분기와 `AI_SPECIAL_REVEAL_DELAY_MS`, 튜토리얼 스크립트의 `commitSharedLocalAction("blue", activate_special)`와 `TUTORIAL_SPECIAL_ACTIVATE_DELAY_MS`를 지웠고, 튜토리얼의 `포위 완료` 안내는 스케줄러가 발동 직전에 `tutorialReactionPhase`를 비우며 끝난다. `?demo=special-pending`에 `&owner=ai`를 더해 AI 소유 특수도 같은 타이머로 도는지 시험에 고정했다. 검증: `npm test` 164개와 브라우저 시험 13개(튜토리얼 장군·외교관·마법사 실습 포함) 통과.
- [CLAUDE CODE] 튜토리얼 성벽 수업 문구에서 `tutorialWallDefensePlaced`(ko)의 `자기 성벽`을 `흑 성벽`으로 바꿨다 — 짝이 되는 상대 성벽 문구가 이미 `백의 성벽에 막혀`라 어느 쪽 성벽인지 명시하는 쪽으로 맞췄다. 일반 규칙을 설명하는 앞 문구 `tutorialWallDefense`("자기 성벽에 닿은 돌은…")는 진영을 가리지 않는 설명이라 그대로 뒀고, 영문 `its own fortress wall`도 유지했다. 브라우저 시험의 기대 문구를 함께 갱신. 검증: `npm test` 164개와 브라우저 시험 13개 통과.
- [CODEX] 1.0 정리 매뉴얼을 만들고 테스트 책임 분리, 내부 진영 ID `black`/`white` 통일, 프로토콜 v3·JSONL 스키마 2 마이그레이션, 실제 두 클라이언트 WebSocket 통합 시험, 미사용 미리보기·에셋·중복 원고 삭제와 온라인 정보 배지 흐름 정리를 완료했다 — 커밋 `dddc87c`·`4d3e7d7`·`5f77feb`·`c3c2cf6`·`bee00e7`·`ff75ce4`; 코드 테스트 161개, 브라우저 테스트 16개, 튜토리얼 연속 2회, 모바일 빌드와 문법·diff 검사 통과.
- [CODEX] 검증된 정리 브랜치를 `main`과 `origin/main`에 fast-forward 병합하고, 완전히 병합된 로컬 브랜치 9개·원격 브랜치 4개를 삭제해 양쪽에 `main`만 남겼다. 루트·엔진·서버 버전을 `1.0.0`으로 통일하고 `docs/RELEASE_NOTES_1_0.md`를 추가했다 — 태그·운영 배포·스토어 제출은 별도 출시 단계로 남겼다.

## [CODEX]

### 의견

- 어제의 왕 성벽 오프닝은 3단계만의 국소 변경이 아니다. 왕을 자기 성벽 가까이에 두고 인접 4칸을 먼저 쌓으면서 1단계의 왕 위험도 입력 분포, 2단계의 성벽 앵커·숨은 특수 반응 국면, 3단계의 지뢰·전환 포획·성벽 돌파 문제 출현 빈도가 함께 달라졌다.
- 1단계는 현재 운영 플래그를 계속 끈 채 회귀만 확인한다. 1a·1c는 마스터 `true_atari` 회귀가 있었고, 1b는 실제 마스터 탐색에 추가 이득이 없었으므로 오늘의 빠른 점검만으로 승격하지 않는다.
- 2단계는 왕 성벽 오프닝과 가장 직접적으로 맞닿지만, 현재 `specialAnchorThreat`가 권위 엔진의 숨은 반응 전이를 끝까지 보존하지 못해 전용 국면 3/3을 실패했다. 오늘은 오프닝이 이 실패를 감추거나 더 자주 만드는지 확인하고 재설계 요구사항을 적는다.
- 3단계는 왕 보호 강화로 `breach_square`는 풍부해졌지만 `conversion_capture`는 0건, `mine_placement`는 5건으로 줄었다. 따라서 어제 이전의 시험 분포를 기준선으로 사용하지 않고, 새 오프닝을 유지할지와 희귀 범주의 표집 방식을 함께 판단해야 한다.
- 캐릭터 연출 아이디어의 기록은 일부 남아 있다. 커밋 `c047ab1`이 `assets/taunts/`에 장군·외교관·마법사의 흑백 이미지 6종을 “special unit cutscenes”로 추가했다. 다만 문서화된 트리거는 왕 성벽 배치뿐이고 현재 `js/render.js`도 왕 이미지만 표시하므로, 특수 능력의 실제 발동 이벤트에 연결하는 상세 동작은 오늘 확정해야 한다.

### AI 플래그 최종 원칙 (사용자 확인 후 갱신)

- 티어 간 반복 대국 승률은 결정론적 수순을 반복하므로 승격의 중심 근거로 쓰지 않는다.
- 플래그는 영구 설정이 아니라 보정을 격리하는 임시 실험 장치다. 검증된 보정은 대상 티어의
  기본 로직에 흡수하고 플래그를 제거한다.
- 시험 결과는 교리 국면과 사람형 비정형 국면을 나눠 본다. 현재의 균등 무작위 배치는 사람형
  분포로 간주하지 않는다.
- 실제 PvE 저널이 충분하기 전에는 매 차례 다섯 티어의 추천 수를 합친 뒤 seed로 선택하는
  `혼합 추천` 분포를 대리군으로 사용한다. 자살수·즉시 패배를 제외한 제한적 실수는 별도 층이다.
- 전체 승률 대신 겨냥한 결정의 개선, 안전 바닥 회귀, 비대상 티어 불변, 티어별 선택 차이의
  설명 가능성을 판정 기준으로 삼는다.

### 1.0 출시 후보 UI 보완

- 튜토리얼 한국어 문구를 교정하고 완료 안내를 두 카드로 분리했다. AI 설정 카드는 난이도
  버튼 3개 높이로 줄였으며, 직접 진영 선택 대신 온라인 대국과 같은 가위바위보로 승자는
  흑·패자는 백을 맡도록 바꿨다. 코드 테스트 161개, 브라우저 테스트 16개, 모바일 빌드와
  실제 브라우저 화면·콘솔 오류 확인을 통과했다.
- 튜토리얼 설명·대화문에 박혀 있던 수동 줄바꿈을 제거하고 화면 폭의 자동 줄바꿈을
  사용한다. 성벽 수업은 판정 대상 흑돌과 맞닿는 성벽 구간을 고정된 금색 빛으로 강조해
  착수 결과 설명까지 유지한 뒤 `다음`에서 전환·해제한다.
- 성벽 강조를 규칙 판정 대상인 B1·B9 흑돌의 실제 접점으로 바로잡고, 백 성벽에는 진한
  외곽 대비를 더했다. 설명·대화의 다음/이전은 오른쪽/왼쪽 화살표와 좌우 움직임으로
  통일했다. AI 난이도 목록은 3px 저채도 스크롤바와 위치 반응형 위·아래 페이드로 바꿨다.

## [CLAUDE CODE]

### 오늘 내 계획 (2026-08-28)

사용자 지시 3항목을 그대로 따른다. 오늘은 **새 기능을 늘리는 날이 아니라, 어제 바뀐 것이
어디까지 번졌는지 확인하고 화면과 연출의 빈칸을 메우는 날**로 잡는다.

#### 0. 기준선 (이미 확인함)

- `npm test` **132개 전부 통과** — 웹 12 · 엔진 116 · 서버 4, 실패 0.
- 브랜치 `main`, 작업 트리는 `TODAY_BRIEFING.md` 외 깨끗.
- 어제 마지막 3커밋(`d0c8517` 왕 교리, `ab33a68` 시험지 재생, `ff1c07d` 원인 규명)까지 반영된 상태.

#### 1. 엔진 — 왕 성벽 오프닝이 1·2·3단계에 미친 영향 점검

바꾼 것은 `js/ai.js`의 티어 설정 두 줄이다(`kingWallDistance` 밴드 + `openingWallStones: 4`,
[js/ai.js:58](js/ai.js:58) 이하 다섯 티어). 이건 평가함수가 아니라 **표집 분포**를 바꾼
변경이므로, 단계별로 영향의 성격이 다르다. 그 구분부터 못 박고 시작한다.

| 단계 | 오프닝의 영향 | 오늘 할 일 |
| --- | --- | --- |
| 1단계 (1a·1b·1c, 전부 OFF) | **간접** — 시험 국면이 바뀌어 `true_atari` 표본이 달라짐 | 새 오프닝 코퍼스로 재채점만. 마스터 회귀(1a·1c −1)가 낡은 분포의 산물이었는지 확인 |
| 2단계 (`specialAnchorThreat`, OFF) | **직접** — 왕이 자기 성벽 옆에 서고 인접 4칸을 쌓으니 성벽 앵커 국면 자체가 흔해짐 | 전용 정답 3국면은 손으로 만든 것이라 오프닝과 무관 → 여전히 3/3 실패인지 재확인. 실제 대국에서 이 국면이 더 자주 나오는지만 센다 |
| 3단계 (특수 공격 숙련) | **직접이자 파괴적** — `conversion_capture` 35→0건, `mine_placement`→5건 | 희귀해진 두 범주를 어떻게 다시 표집할지 결정. 오프닝을 되돌릴지 여부의 판단 근거를 적는다 |

- **비싼 재표집은 하지 않는다.** 저장된 코퍼스에 `--load-exam` + `--tier-override`를 걸면
  설정당 5초다(재표집은 45분). 손으로 만든 정답 국면은 `npm run ai:probe-objectives`.
- 오늘의 산출물은 **코드가 아니라 판정**이다: 1a·1b·1c·2단계를 계속 OFF로 둘 근거가
  새 분포에서도 유지되는가, 그리고 3단계의 다음 검증 대상은 무엇인가.
- 예상되는 결론 하나를 미리 적어둔다 — 3단계에서 드러난 "지연된 사실을 깊은 탐색이 할인한다"는
  병은 오프닝을 되돌려도 사라지지 않는다. 오프닝은 그 병을 **덜 보이게** 만들 뿐이다.
  따라서 오프닝 유지/철회는 난이도 설계의 문제로 다루고, 결함 수정과 섞지 않는다.

#### 2. 한영 문구·디자인 점검 + 초심자 튜토리얼

문구는 전부 [js/config.js](js/config.js)의 `TEXT.en` / `TEXT.ko` 한 쌍이다. 눈으로 훑는 대신
**실제로 렌더링해서 폭을 잰다** — 한국어가 영어보다 짧아 보여도 줄바꿈 지점과 버튼 폭은 다르다.

- 브라우저 창에서 375px(모바일) · 768px · 데스크톱 폭으로 **한/영 각각** 캡처: 로비 → 모드 선택
  → AI 난이도 카드 → 대국 화면 상단 상태줄 → 팝업(자살수 경고, 특수 발동, 패스, 결과 카드).
- 위험 구간을 미리 지목해둔다 — 긴 문자열이 좁은 그릇에 들어가는 곳이다:
  - `kingTerritoryBlocked`, `kingOverlapBlocked` (두 줄짜리 규칙 설명이 토스트에 들어감)
  - `pveGrandmasterDescription` 등 난이도 카드 5종 (카드 높이가 서로 어긋나기 쉬움)
  - `activateSpecialPrompt` / `activateSpecialButton` — `{unit}` 치환이라 한국어 조사가 어긋남
  - `turnWithTime`, `noLegalMoves`, `boardFilled` — 숫자 치환이 있는 상태줄
- 영어 쪽 어색함도 같이 본다. 예: `tauntBubble: "Chicken!!"` 은 한국어 "쫄!!"의 번역으로는
  뜻이 통하지만 말투 온도가 다르다. 이런 항목은 고치기 전에 목록으로 먼저 올린다.
- **튜토리얼**은 [app.js:168](app.js:168) 이하 `TUTORIAL_STEPS` 8단계다. 현재 설명은
  "무엇을 하라"만 있고 **"왜 그런지"가 빠져 있다.** 초심자가 막히는 지점 순서로 보강한다:
  1. 왕을 먼저 두는 이유와 성역이 몇 턴 가는지(특수 5턴 / 일반 4턴의 차이가 지금 한 줄에 묻힘)
  2. 성벽이 **방어에도 공격에도** 쓰인다는 대칭 — 지금 두 단계가 붙어 있지만 대비가 약함
  3. 특수 3종은 "포위당하면 발동"이라는 공통 규칙이 먼저, 개별 효과는 그 다음
- 검증은 `npm run test:browser`의 Challenge/튜토리얼 흐름으로 한다(이미 커버 중: `f5a5d44`).

#### 3. 캐릭터 아트 — 특수 능력 발동 시 이미지 표시

`c047ab1`이 넣은 6종(`assets/taunts/{general,diplomat,wizard}{b,w}.png`)은 **아직 어디에서도
참조되지 않는다.** [js/render.js:73](js/render.js:73)이 왕 두 장만 하드코딩한다.

현재 왕 도발이 도는 경로를 그대로 따라가면 된다 — 이미 권위 이벤트로 설계돼 있다:

```
actions.js:70  state.tauntEvent = { id, speakerOwner, targetOwner, row, col }
actions.js:89  emitEvent → { type: "taunt_used", ... }
protocol.js:123  tauntEvent/tauntSerial/tauntUntil 검증 → 온라인 동기화됨
app.js:1385  presentSharedLocalEvents → showTauntBubble   (로컬/PvE)
app.js:2294  state.tauntEvent.id 비교 → showTauntBubble    (온라인)
render.js:65 createTauntOverlay → 이미지 + 말풍선
```

붙일 발동 시점은 새로 만들 필요가 없다. **엔진이 이미 이벤트를 쏘고 있다**:

| 이벤트 | 유닛 | 의미 |
| --- | --- | --- |
| `special_activated` | 셋 다 | 포위된 특수가 능력 발동 |
| `piece_removed` (`reason: "general_reaction"`) | 장군 | 인접 적 제거 |
| `pieces_converted` | 외교관 | 인접 적 전향 |
| `wizard_move_required` | 마법사 | 순간이동 시작 |

가장 단순한 안은 **`special_activated` 하나에만 거는 것**이다. 셋 다 이 이벤트를 지나가고,
유닛별 후속 이벤트(제거/전향/이동)는 연출 이후에 이어진다. 세 이벤트에 각각 걸면 장군은 적을
여러 개 지울 때 연출이 여러 번 뜬다.

**먼저 확인해야 할 제약 세 가지** — 이게 오늘 점검의 핵심이고, 그냥 이미지만 갈아끼우면 깨진다:

1. **왕 도발은 배포를 3초 잠근다.** `tauntUntil`이 그 잠금이고 서버 테스트가 이를 고정하고
   있다(`an automatic King-wall taunt locks deployment for exactly three seconds`).
   특수 발동 연출까지 같은 잠금을 쓰면 턴 진행이 3초씩 밀린다. 특히 마법사는 발동 직후
   **사람이 이동 칸을 골라야 해서** 잠금과 입력이 겹친다.
   → 연출(보이는 것)과 잠금(규칙)을 **분리한다.** 왕만 잠금을 유지하고, 특수 발동은 표시 전용.
2. **숨은 정체가 새면 안 된다.** `events.js:23` `eventsForPlayer`가 미공개 특수의 `unitType`을
   상대에게 `soldier`로 가린다. 연출 이벤트가 `unitType`을 실어 나르므로, 반드시
   `special_revealed` **이후**에만 나가는지 확인한다. (`special_activated`는 공개 이후라 안전할
   것으로 보이나 테스트로 못 박는다.)
3. **화면 회전.** `styles.css:2520` `.fortress-frame.view-red .taunt-overlay { rotate: 180deg }` —
   흑 시점에서 판이 뒤집히므로 새 이미지도 같은 규칙을 타야 한다.

오늘의 완료 조건은 **구현까지가 아니라 여기까지**로 잡는다: 위 제약 3개를 확인하고, 이벤트
1개(`special_activated`)에 거는 설계를 확정하고, 회귀 테스트가 무엇을 고정해야 하는지 적는다.
시간이 남으면 로컬 모드부터 붙이고 PvE·온라인은 같은 이벤트를 쓰므로 자동으로 따라온다.

#### 오늘 하지 않을 것

- 새 AI 정책·플래그 승격 (1단계는 판정만)
- 대용량 JSON 정리 / `.gitignore` (어제 논의됐고 오늘 순서에 없음)
- `terminalObjectiveModel` 계열 — `docs/AI_MODEL.md` 기준 의도적 파킹 상태

### 의견 요청 — `docs/AI_FLAG_STRUCTURE.md`

리그를 돌리려다 멈췄다. **설정들이 서로 독립이 아니어서** 지금 리그를 돌리면 퇴화한 조합을
20pp 해상도로 재게 된다. 구조부터 정리하는 제안서를 `docs/AI_FLAG_STRUCTURE.md`에 썼다.

교리 시험지 위에서 잰 결정 단위(28문제 × 5티어 = 140) 비교 결과:

| 가설 | 결과 |
| --- | --- |
| 1c 단독 = `kingTacticalPriority: 0` | **0/140 다름** — 새 목적함수가 아니라 스위치였다 |
| 2단계 단독 = 무효 | **0/140 다름** — 1a 없이는 `kingDangerValue`가 호출조차 안 됨 |
| 1b 단독 = 무효 | **0/140 다름** — 지수 12와 블렌드 ×0.1에 두 겹으로 눌림 |
| *대조군* 1a | 4/140 다름 — 비교 방법은 작동한다 |

요지는 셋이다.

- **버릴 것 3개** — `terminalObjectiveModel`(1a·1b·1c 별칭), `specialAnchorThreat` 최상위 노출,
  `searchAlgorithm` 티어 노출. 운영 동작 변화 0.
  (`ironcladKingDefense`도 처음엔 여기 있었으나 **철회했다** — 읽는 곳이 0곳인 건 사실이지만
  보정 스택의 틀에서는 죽은 설정이 아니라 **배선이 빠진 보정**이다.)
- **축은 다섯이 아니라 둘** — A축(왕 위험 측정: 1a + 2단계) · B축(결합: 1b + 1c를 하나로).
  조합이 7에서 4로 줄고 퇴화 셀이 사라진다.
- **정정** — 초판에 "4단계 교리와 평가함수가 모순"이라고 썼는데 **내 측정 착오였고 철회했다.**
  실제로 각 티어에게 자기 교리로 오프닝을 두게 하면 왕 활로는 7~8이지 1이 아니다(왕을 자기 돌로
  감싸면 **그룹이 커져 활로가 늘어난다**). 그리고 밴드가 허가라 초급 0~6 · 중급 0~5 · 고급 0~4는
  중앙 4:4를 포함해서 **세 티어는 교리 전과 같은 자리에 왕을 둔다** — 실제로 움직인 건
  전문(3:4)과 마스터(1:4) 둘뿐이다. 남는 질문은 모순이 아니라 **난이도 설계 의도**다:
  하위 세 티어에서 밴드가 비구속적인 것이 의도인가, `openingWallStones: 4`가 다섯 티어
  동일한 것이 의도인가.

**설계 의도 확인 후 갱신 (사용자):** 플래그는 독립 가설이 아니라 **보정 스택**이다 —
초급은 날것, 상위 티어로 갈수록 튜닝되고, 튜닝이 겁쟁이를 만들면 다음 플래그가 그것을 상쇄한다.
코드가 실제로 그렇다: `criticalBeliefWorldLimit`가 1 → 6으로 뛰는 티어(고급)에서 겁쟁이 보정
`instantWinRiskTolerance 0.7`이 정확히 켜진다. 거울(`specialAssaultTargets`)도 이미 구현돼
전문·마스터에 켜져 있다 — `docs/AI_MODEL.md`가 "코드에 전혀 없다"고 한 것은 낡았고 갱신했다.

이 틀에서 **오늘 내 측정 절반이 잘못된 질문을 물었다.** `--tier-override`는 다섯 티어 전부에
같은 값을 적용하므로, "1a를 켰다"는 곧 **초급에게도 켠 것**이다. 사다리가 아니라 평평한 판을
쟀다. 퇴화 증거(0/140 세 건)는 영향받지 않지만, 1a의 "−1 회귀"처럼 부호를 읽은 결과는 다시
물어야 한다. 도구에 티어 범위가 필요하다.

사다리가 끊긴 곳도 하나 찾았다 — **`openingWallStones: 4`가 다섯 티어 동일**해서 초급이
날것이 아니다. 실측한 초급의 첫 다섯 수는 `king@4:4 soldier@3:4 soldier@5:4 soldier@4:3
soldier@4:5`로 **교과서적 완전 링**이며, 마스터와 왕 자리만 다르고 구조가 같다.
`variance: 1.5`는 링 안에서 순서만 흔든다.

**[CODEX]·[GOOGLE ANTIGRAVITY]에게** — 문서 끝의 질문 4개에 각자 구역에서 답해 주기 바란다.
특히 `ironcladKingDefense`는 앞서 "삭제 제안"이라고 썼으나 **철회한다** — 보정 스택의 자리에
이름이 맞으므로 기획됐다가 배선이 빠진 보정으로 보인다. 원래 의도를 아는 쪽이 있으면 알려 달라.

### 메모

- 어제(2026-08-27) 기록은 `오늘의 작업 기록`에서 지웠다. 내용은 소실되지 않는다 —
  엔진·측정 관련 실질은 `docs/AI_MODEL.md`에, 나머지는 git 히스토리(`b754c28`·`4464bc0`·`ff1c07d`)에 남아 있다.
- 다른 AI 구역은 공동 편집 규칙(다른 AI 구역은 수정·삭제하지 않는다)에 따라 손대지 않았다.
  `[GOOGLE ANTIGRAVITY]`가 그 사이 자기 구역을 오늘 계획으로 갱신했고, 4번(아트) 전담과
  트리거 설계(`special_activated` 단일 진입점 · 잠금 분리 · 정보 은닉 · 화면 회전)가 위
  역할 분담표와 일치한다. **다만 그쪽은 오늘 구현까지 예정하고 있다** — 역할 분담표에 적은
  '뒤집을 조건'에 해당하므로, 담당을 유지할지 사용자 판단이 필요하다.

### 1 엔진 점검 결과 (2026-08-28, 읽기 전용 분석)

운영 코드 0줄 변경. 모든 산출물은 저장소 밖 스크래치패드에 두었고, 실행 중 더럽혀진
`experiments/objective-model-tactical-validation.json`은 `git checkout`으로 되돌렸다.

#### 결론 먼저

1. **어제 1a·1c를 보류한 근거(마스터 `true_atari` −1 회귀)는 재현되지 않았다.** 새로 뽑은
   코퍼스에서 1a·1c 모두 마스터 **+2 / −0**이다.
2. **그렇다고 승격을 권고하지 않는다.** 어제의 −1도 오늘의 +2도 12문제 표본에서 어느 12개가
   뽑혔느냐의 산물이다. 이 시험은 1a·1c를 판정할 해상도가 없다.
3. **오늘의 진짜 발견은 시험지 쪽이다** — 어제 3단계에서 진단·수정한 표집 결함이
   1·2단계 시험에는 그대로 남아 있다.

#### 발견 1 — 오프닝 수정이 두 시험 중 하나에만 적용됐다

| | 1·2단계 시험 `ai-tactics-suite.mjs` | 3단계 시험 `probe-special-doctrine.mjs` |
| --- | --- | --- |
| 첫 배치들 | `ply < --opening`(기본 6) 구간 **균등 무작위** | `placed < DOCTRINE_DEPLOYMENTS`면 `askTier` (교리) |
| 왕의 자리 | **항상 무작위** | 티어의 `kingWallDistance` 밴드 |
| 어제 수정 | 안 됨 | 됨 (`ab33a68`) |

왕이 항상 무작위인 이유는 표집기 혼자만의 문제가 아니다. `packages/game-engine/src/board.js`의
`canDeployPosition`이 `(state.firstDeployDone[player] || type === "king")`으로 **첫 배치를 왕으로
강제**한다. 따라서 무작위 오프닝 구간이 1플라이만 있어도 왕은 무작위로 놓이고,
`openingWallPlan`의 `kingWallDistance` 게이트는 한 번도 조회되지 않는다.

4게임 표본의 실측 왕 성벽 거리 — 흑 `{4, 5, 6}`, 백 `{1, 3}`. 마스터 밴드는 **1 고정**,
전문 0~3, 고급 0~4다. 관측된 5개 배치 중 마스터가 둘 자리는 하나뿐이었다.

**그런데 절반만 무작위다.** 배치 3·4번째는 플라이 6 이후라 `askTier`를 타고,
`openingWallPlan`의 `openingWallStones` 링 계획이 그대로 적용된다. 결과는
**무작위 왕 + 그 주위에 교리적으로 쌓은 성벽**이라는 잡종이다. 어떤 티어도 두지 않을 국면이며,
수정 전 분포도 아니고 수정 후 분포도 아니다.

그 증거가 범주 구성의 이동이다. 같은 seed·같은 게임 수인데:

| | 어제 (수정 전) | 오늘 (수정 후) |
| --- | ---: | ---: |
| 표집 플라이 | 368 | 376 |
| 총 문제 | 36 | 29 |
| `must_defend` | 12 (상한 포화) | **3** |
| `true_atari` | 12 (상한 포화) | 12 (상한 포화) |
| `win_now` | 6 | 5 |
| `avoid_suicide` | 6 | **9 (상한 포화)** |

**따라서 `docs/AI_MODEL.md`의 "오프닝이 바뀌었으므로 이전 측정치는 전부 낡은 분포의 것이다"는
너무 넓은 진술이다.** 3단계에는 참이지만, 1·2단계 시험은 애초에 교리 오프닝을 쓴 적이 없다.
정확히는 — 1·2단계의 옛 측정치는 *낡은* 게 아니라 *처음부터 오프-디스트리뷰션*이었고,
어제 변경으로 **다른 종류의** 오프-디스트리뷰션이 됐다.

#### 발견 2 — 1단계 변형 스윕 (재생성 코퍼스, 29문제 × 5티어 = 145결정)

seed 20260827, 8게임, `--soldiers-only --per-category 12`. 기초 3범주
(`must_defend` 3 / `win_now` 5 / `avoid_suicide` 9)는 **모든 변형·모든 티어에서 만점**이다.

`true_atari` (12문제 중):

| 변형 | 초급 | 중급 | 고급 | 전문 | 마스터 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 기준선 | 5 | 6 | 6 | 2 | 6 |
| 1a | 5 | 6 | 6 | **5** | **8** |
| 1b | 5 | 6 | 6 | 2 | 6 |
| 1c | 5 | 6 | 6 | **6** | **8** |
| 1a+1b | 5 | 6 | 6 | 5 | 8 |
| 1a+1c · 1b+1c · 전체 | 5 | 6 | 6 | **6** | **8** |

국면 단위로 쪼개면 총계보다 훨씬 많은 것이 보인다:

| 변형 | 바뀐 결정 / 145 | 어느 티어에서 | 전문 | 마스터 |
| --- | ---: | --- | --- | --- |
| 1a | 23 | 초급 1 · 중급 1 · 고급 2 · 전문 11 · 마스터 8 | **+4 / −1** (p18 상실) | +2 / −0 |
| 1b | **0** | — | +0 / −0 | +0 / −0 |
| 1c | 18 | **전문 9 · 마스터 9만** | **+4 / −0** | **+2 / −0** |

읽을 것 세 가지:

- **1c는 외과적이다.** 건드려야 할 두 티어만 건드리고, 잃는 국면이 하나도 없다.
- **1a는 새는 데가 있다.** 손대지 않아야 할 하위 3티어의 결정 4개를 바꾸고, 전문에서 p18을 잃는다.
  총계 표에서는 둘 다 "+"로만 보이지만 성격이 다르다.
- **1b는 145개 결정 중 0개를 바꾼다.** 어제와 오늘, **서로 다른 두 코퍼스에서 모두 정확히 0**이다.
  "효과를 검출하지 못했다"가 아니라 "이 시험이 묻는 것에 대해 1b는 아무 것도 하지 않는다"로
  읽는 편이 이제 더 정확하다.

#### 발견 3 — 2단계는 판정 불변, 이유는 기록보다 날카롭다

`npm run ai:probe-objectives` 출력이 어제 파일과 **`generatedAt` 한 줄만 빼고 동일**했다.
손으로 만든 국면이라 오프닝과 무관하다는 예상이 그대로 확인됐다.
판정: `territoryVerdictModel: hold`, `specialAnchorThreat: redesign`.

다만 "3/3 실패"의 범위를 좁혀 적어야 한다. `modelPassesAnswerKey`는 **isolated 팔**
(깊이 1, 믿음 기구 off)만 본다. production 팔에서는 기준선과 모델이 **둘 다 안전수
`soldier:8:8`을 고른다.** 즉 2단계가 겨냥한 국면에서 **평가항이 놓치는 것을 탐색이 이미 덮고 있다.**
이건 보류 근거로 오히려 더 강하다 — 이 국면에서 플래그의 운영상 이득이 0으로 측정된 것이다.

측정 주의 하나: 이 probe의 production 팔은 `kingDangerModel: true`(1a ON)로 돈다.
**1a는 출시 기본값이 아니므로 그 팔은 우리가 배포하는 설정을 재고 있지 않다.**

3단계와의 대비가 유용하다. 여기선 깊이가 **돕는다**(한 수 뒤 왕이 잡히는 즉시 위협).
`breach_square`에서는 깊이가 **해친다**(보상이 지연되고 상대가 반응을 거절할 수 있다).
같은 축의 반대 부호이며, "지연된 사실"이라는 진단과 모순되지 않는다.

#### 발견 4 — 3단계는 재측정이 불필요하다

3단계 시험지는 이미 교리 오프닝을 쓴다(`ab33a68`). 따라서 어제 기록한 수치
(`breach_square` 티어별 63·64·51·38·43%, `conversion_capture` 0건, `mine_placement` 5건)가
**현재 분포 그대로**다. 오늘 다시 45분을 쓸 이유가 없다.

#### 그래서 무엇을 할 것인가

- **1a·1b·1c·2단계 모두 OFF 유지.** 어제와 결론은 같지만 근거가 바뀌었다. 어제는
  "마스터 회귀 때문에"였고, 오늘은 **"이 시험에 판정할 해상도가 없기 때문에"**다.
- **다음 검증 대상은 정책이 아니라 시험지다.** `ai-tactics-suite.mjs`의 표집을
  `probe-special-doctrine.mjs`와 같은 모양으로 맞춘다 — 교리가 첫 5배치를 갖고, 그 뒤에
  `--random-plies`로 변화를 준다. 그래야 1·2단계가 **티어가 실제로 만드는 국면**에서 측정된다.
- 그 수정 뒤에야 1c 승격 여부를 물을 수 있다. 지금 데이터로는 1c가 유망해 보이지만
  (외과적, 손실 0), 잡종 분포에서 나온 +2는 승격 근거가 못 된다.
- 부수 정비: `probe-objective-model-tactics.mjs`는 출력 경로가 하드코딩이라
  (`scripts/probe-objective-model-tactics.mjs:14`) 실행할 때마다 `experiments/`의 추적 파일을
  더럽힌다. 다른 두 스크립트처럼 `--output`을 받게 하는 편이 좋다.

#### 편집 차례에서 한 일 — 그리고 위 판정이 뒤집힌 지점

사용자가 아트보다 먼저 편집권을 줬다. 시험지를 고치고 **고친 시험지로 다시 쟀더니
위 `발견 2`의 결론이 뒤집혔다.** 잡종 분포에서 나온 수치였으므로 예상된 위험이었고,
실제로 부호가 반대였다.

`true_atari` 12문제, 같은 seed·같은 8게임:

| | 잡종 시험지 | 교리 시험지 |
| --- | --- | --- |
| 기준선 전문 / 마스터 | 2 / **6** | 7 / **11** |
| 1a | +3 / +2 | **−1 / −1** |
| 1c | +4 / +2 | **0 / 0** |
| 1b | 0 | 0 |

- **잡종 시험지가 상위 티어를 실제보다 못하게 보이게 했다** — 마스터 기준선 6 → 11.
- **1a의 회귀는 진짜였다.** 08-27의 마스터 −1이 올바른 분포에서 재현됐다.
- **1c는 정확히 중립이다.** 140결정 중 10개의 수를 바꾸되 정답 수는 0개 바꾼다.
  어제 `발견 2`에서 '유망하다'고 적었던 것은 잡종 분포의 산물이었다.
- 1b는 **세 코퍼스 연속 0**.

따라서 최종 판정은 **1a·1b·1c·2단계 전부 OFF 유지**로 08-27과 같지만, 이제 티어가 실제로
만드는 국면에서 잰 근거다. 상세는 `docs/AI_MODEL.md`의
`1·2단계 시험지도 같은 병을 앓고 있었다 (2026-08-28)`.

## [GOOGLE ANTIGRAVITY]

### 오늘 내 계획 (2026-08-28)

사용자께서 제안해주신 대로 **4번 (캐릭터 아트 발동 연출 점검 및 연동)**을 전담하여 진행하겠습니다.

#### 1. 캐릭터 연출 관련 이전 기록 및 현재 상태 확인

- **기록 확인**: 커밋 `c047ab1`(`feat(assets): add special unit cutscenes and update king taunt illustrations`)에서 장군·외교관·마법사의 흑/백 일러스트 6종(`assets/taunts/{general,diplomat,wizard}{b,w}.png`)이 에셋으로 추가되어 있습니다.
- **현재 구현 상태**: 현재 [js/render.js:73](js/render.js:73)에는 왕 도발 2종(`kingb_zzol.png`, `kingw_zzol.png`)만 하드코딩되어 있으며, 특수 유닛 6종 이미지는 아직 렌더러와 연결되지 않은 상태입니다.

#### 2. 에셋 매핑 및 연출 아키텍처 조사 결과

| 기물 (unitType) | 흑 (red) 에셋 | 백 (blue) 에셋 | 규격 | 발동 트리거 이벤트 |
| --- | --- | --- | --- | --- |
| `general` (장군) | `assets/taunts/generalb.png` | `assets/taunts/generalw.png` | 859×1024 | `special_activated` |
| `diplomat` (외교관) | `assets/taunts/diplomatb.png` | `assets/taunts/diplomatw.png` | ~815×1024 | `special_activated` |
| `wizard` (마법사) | `assets/taunts/wizardb.png` | `assets/taunts/wizardw.png` | ~810×1024 | `special_activated` |
| `king` (왕 도발) | `assets/taunts/kingb_zzol.png` | `assets/taunts/kingw_zzol.png` | 1024×1024 | `taunt_used` (성벽 배치 시) |

- **신규 텍스트 키 필요 여부**:
  - 특수 기물 연출은 기존의 검증된 `UNIT_LABELS[unitType]` (장군, 외교관, 마법사 / General, Diplomat, Wizard)을 배지/라벨로 표시하거나 순수 일러스트 컷씬으로 띄울 수 있어, `js/config.js`에 필수적인 신규 텍스트 추가 없이 동작 가능합니다. (Codex가 한영 작업 중 전용 멘트를 추가할 경우 연동 가능)

#### 3. 4번 작업: 캐릭터 아트 발동 연출 설계 및 구현 계획

- **트리거 시점 단일화**:
  - 엔진의 `special_activated` 이벤트를 단일 진입점으로 활용합니다. (장군의 개별 포획이나 외교관 전향 등 후속 세부 이벤트마다 중복 발동되는 현상 방지)
- **주요 제약 해결 설계**:
  1. **잠금 분리 (Non-blocking 연출)**: 왕 도발의 3초 배포 잠금(`tauntUntil`, `visibleTaunt`)과 달리, 특수 발동 연출(`visibleCutscene`)은 게임 흐름과 유저 인터랙션(특히 마법사의 순간이동 탈출 좌표 선택 `selectCell`)을 차단하지 않는 **비차단 오버레이(약 1.5~1.8초 표시 후 자동 소멸)**로 분리 구현합니다.
  2. **정보 은닉 보장**: `special_revealed` 및 `special_activated` 시점에만 정체가 공개되므로, 미공개 상태에서 상대에게 이미지가 노출되지 않도록 기존 엔진의 이벤트 은닉 규칙(`eventsForPlayer`, `stateForPlayer`)을 철저히 준수합니다.
  3. **화면 회전 대응**: 흑/백 플레이어 시점(`view-red`)에 맞추어 CSS 오버레이 회전(`.fortress-frame.view-red .taunt-overlay { rotate: 180deg }`) 및 위치가 자연스럽게 표시되도록 스타일을 적용합니다.
  4. **모드 일관성**: 로컬 플레이, PvE, 온라인(WebSocket) 환경 모두에서 동일한 이벤트/상태 흐름으로 컷씬이 표시되도록 연동합니다.

#### 4. 협업 순서 및 편집권 준수

- 실행 순서(1. Claude Code → 2. Codex → 3. Google Antigravity)에 따라 사전 조사를 완료하고 대기하며, 앞선 커밋이 완료되면 편집권을 넘겨받아 `js/render.js`, `styles.css`, `app.js`의 연출 영역을 구현하고 독립 커밋 및 검증을 진행하겠습니다.
- [CODEX] 튜토리얼 인트로 대화창 좌측 상단의 노란 `✦` 장식을 제거했다 — 우측 하단 진행 화살표와 기존 클릭 동작은 그대로 유지하고 브라우저 시험으로 카드 전환 흐름을 고정했다.
- [CODEX] 튜토리얼의 별도 “튜토리얼 완료!” 카드를 제거하고 흑 왕 포획 승리 안내를 최종 카드로 만들었다. 승리 직후 `다음` 대신 인트로의 `시작하기`와 같은 스타일의 `나가기` 버튼을 표시하고, 누르면 대국 선택 로비로 돌아가도록 연결했다 — 전체 164개 테스트와 튜토리얼 전체 브라우저 시험, `node --check app.js`, `git diff --check` 통과.
- [CODEX] 최종 튜토리얼 카드에서 흑 왕 포획 승리, 배운 규칙 요약, AI 대전의 다음 목표를 한 문단으로 합쳐 두 핵심 안내를 모두 보존했다.
- [CODEX] 설명 카드의 `시작하기`, `나가기`, `다음` 동작을 대화창과 같은 우측 하단으로 통일하면서 `tutorial-actions`의 32px 높이와 버튼별 스타일을 유지했다. 일반 대국의 마법사 이동 선택과 챌린지 결과 버튼은 기존 중앙 정렬을 유지했다.
- [CODEX] PvE·온라인 대국 종료의 자동 전환 시간을 없애고, 마지막 보드 위 상단 카드에 승자 돌·승리 문구·종료 이유와 `경기 결과`·`다시 대국`·홈을 계속 표시하도록 재구성했다. `경기 결과`를 누르면 중앙 상세 카드에 대국 종류·난이도·진영, 최종 흑백 유닛 수, 종료 방식, 총 배치 수, 포획 기록, 특수 유닛 사용 수와 포획이 점수가 아니라는 안내가 나온다 — 전체 164개 테스트와 종료·튜토리얼 브라우저 시험 5개 통과, 상세 카드가 작은 화면 안에 들어오는 실제 위치까지 검증했다.
- [CODEX] 상단 대국 종료 카드를 2열로 재구성했다. 중앙에는 승자 돌·`흑 승리`·`대국 종료: 흑 n - 백 n`을 정렬하고, 오른쪽 프레임에 `경기 결과`·`다시 대국` 버튼을 세로로 묶었다. 홈 이동은 중복 버튼을 만들지 않고 기존 하단 홈 버튼으로 유지했다.
- [CODEX] 종료 카드의 승자 돌을 키우고 승리 문구와의 기존 위치를 유지했다. `대국 종료: 흑 n - 백 n`은 카드 전체의 가로 중앙으로 독립시켰고, 오른쪽 두 버튼은 프레임 내부 비율에 맞게 높이를 늘렸다.
- [CODEX] 왕 포획 승리 시 포획된 왕의 원래 진영·위치를 종료 상태에 기록하고, 최종 보드에서 원래 색과 왕 문양으로 표시하도록 했다. 규칙 상 포획 칸은 여전히 승자 측 병사로 처리되어 유닛 수·포획 기록·리플레이에는 변화가 없다.
- [CODEX] 상단 종료 카드에서 승자 돌·승리 문구와 `대국 종료: 흑 n - 백 n`의 가로 축을 왼쪽 결과 영역 중앙으로 통일했다. 오른쪽 버튼 프레임과 독립된 정보 덮으로 보이도록 정렬했다.
- [CODEX] AI 대국 5단계의 한국어 표시명을 `삼류 고수`·`이류 고수`·`일류 고수`·`절정 고수`·`초절정 고수`로 되돌렸다. 내부 AI 키(`novice`~`grandmaster`)와 성능 규칙은 변경하지 않고, 난이도 선택·상단 표시·결과 메타·튜토리얼 안내의 명칭과 띄어쓰기를 통일했다.
- [CODEX] 병사·장군·외교관·마법사 배치 버튼에서 잠금·사용 완료 상태에 따른 동적 재정렬을 제거해 항상 같은 2×2 위치를 유지하게 했다. 상태별 비활성·잠금·사용 완료 표시는 그대로 보존하고, 사용 완료된 장군이 두 번째 자리를 지키는 브라우저 회귀 시험을 추가했다 — 전체 코드 테스트 161개와 관련 브라우저 흐름 10개, 실제 인앱 화면 확인 및 `git diff --check` 통과.
